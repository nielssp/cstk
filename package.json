{
  "name": "cstk",
  "version": "0.6.0",
  "description": "The Classic Stylesheets Toolkit",
  "author": "Niels Poulsen",
  "license": "MIT",
  "repository": "nielssp/cstk",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc -p ./tsconfig.json",
    "watch": "tsc -w -p ./tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest"
  },
  "dependencies": {
    "cytoplasmic": "^0.10.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
